# AGENTS.md — دليل العمل على مشروع "زبون"

مرجع إلزامي لكل agent يشتغل على هذا الريبو (بما فيهم أنا في المحادثات الجاية). اقرأه قبل أي مهمة والتزم به.

---

## 1. نظرة عامة

زبون — منصة متاجر محلية (سوق متعدد البائعين) لمنطقة الكوت/واسط: زبون يشتري من المتاجر، تاجر يدير متجره ومحفظته، مندوب يوصل الطلبات، وأدمن يدير المنصة. الهدف: منصة متكاملة تعمل على تطبيق موبايل + ويب، ويتم النشر مباشرة للسحابة.

## 2. الـ Stack التقني

- **backend/** — Node.js (ESM، `"type": "module"`) + Express 5 + PostgreSQL (مكتبة `pg`) + JWT (`jsonwebtoken`) + `bcryptjs` + `grammy` (بوت تليجرام لـ OTP) + `sharp` (معالجة صور) + `cors` + `dotenv` + `socket.io` (مثبتة لكن الاستخدام محدود).
- **app/** — Flutter/Dart: `http`, `shared_preferences`, `image_picker`, `url_launcher`, `share_plus`, `flutter_map`+`latlong2`, `geolocator`, `lottie`, `flutter_local_notifications`, `workmanager` (جلب إشعارات خلفي كل 15 دقيقة). Lints: `flutter_lints`.
- **store-app/** — Vite + React 18 + `react-router-dom` + `leaflet` (واجهة المتجر للويب).
- **admin-dashboard/** + **landing/** — HTML/JS/CSS ثابت بدون فريمورك (اللوحة مبنية بـ `innerHTML` يدوياً).
- **قاعدة البيانات:** PostgreSQL — الإنتاج على Neon سحابة، والتطوير محلياً في `infra/pgdata` على المنفذ **5434**.
- **النشر:** Vercel (`backend/vercel.json` → `backend/api/index.js` → `src/app.js`).

## 3. بنية المجلدات

- `app/` — تطبيق Flutter (موبايل): `lib/core/` (api, models, widgets, theme, routing, notifications) + `lib/features/<feature>/screens/`.
- `backend/src/` — كود السيرفر: `routes/` (auth, customer, vendor, delivery, admin, public, uploads, routing, telegram)، `app.js` (جذر التطبيق)، `server.js` (الإقلاع المحلي)، `db.js` (اتصال + `q`/`one`/`tx`)، `bootstrap.js` (تهيئة آمنة للقاعدة)، `schema.sql`، `seed*.js`.
- `backend/src/public/` — الملفات الثابتة المُقدَّمة: `store/`, `landing/`, `uploads/`.
- `backend/public/admin/` — نسخة لوحة الأدمن المُقدَّمة فعلياً؛ و`admin-dashboard/` المصدر — **أي تعديل على اللوحة يجب أن ينطبق على الاثنتين معاً**.
- `store-app/` — واجهة المتجر React (بنيتها `npm run build` ثم نسخ الـ dist إلى `backend/src/public/store/`).
- `landing/` — صفحة الهبوط + مجلد `downloads/` (APK الرسمي للمستخدمين).
- `infra/` — قاعدة PostgreSQL المحلية (pgdata).
- `scripts/publish.sh` — سكربت النشر الرسمي للنسخ (انظر الأوامر).
- `SECURITY_AUDIT.md` — تقرير الثغرات المعروف وأولوياتها (راجعها عند أي تعديل أمني).

## 4. أوامر التشغيل

من `backend/`:
- `npm run dev` — سيرفر تطوير مع `--watch` على :4000
- `npm start` — تشغيل عادي
- `npm run db:init` · `npm run db:migrate` · `npm run db:seed` — إنشاء/ترحيل/بذر القاعدة
- النشر: `vercel --prod` (من داخل `backend/`)

من `app/`:
- `flutter analyze` — **الفحص الإلزامي قبل أي تسليم** (بوابة الثبات الساكن)
- `flutter test` — موجودة لكنها حالياً مكسورة/تشير لملفات محذوفة (لا تعتمد عليها كبوابة)
- **التحقق العملي المعتمد:** تشغيل التطبيق (`flutter run` على جهاز/محاكي) وتجربة الفيتشر يدوياً — هذا هو المرجع الفعلي لصحة السلوك، مو `flutter test`
- `flutter build apk --release` — بناء APK (المخرج: `app/build/app/outputs/flutter-apk/app-release.apk`)
- النشر الكامل للنسخة (لا تفعله إلا بطلب): `./scripts/publish.sh <version> "وصف"` — يرفع الإصدار في `states.dart` + `app-version.json`، يبني APK، ينسخه لـ `landing/downloads/` و`backend/public/landing/downloads/`.

من `store-app/`: `npm run dev` (تطوير) · `npm run build` (بناء dist ثم نسخها لـ `backend/src/public/store/`).

**انتبه للتشغيل المحلي:** خادم :4000 يقرأ `backend/.env` الذي يشير إلى قاعدة **الإنتاج** (Neon). للتطوير ضد القاعدة المحلية: `DATABASE_URL='postgres://zaboon@127.0.0.1:5434/zaboon' PGSSL=false PORT=4000 npm start`.

**حسابات العرض:**
- محلية فقط (لا توجد بالإنتاج): أدمن `07900000000`/`admin123`، مندوب `07705556677`/`vendor123`
- ⚠️ تاجر/زبون/مندوب تجريبي `000000000100/200/300` (كلمة `123456`) **موجودة حالياً على قاعدة الإنتاج** — تُعدّ اعتماداً مُعرَّضاً للعموم: لا تعتمد عليها كـ"حسابات محلية آمنة"، ولا تستخدمها لاختبار كتابة على الإنتاج، وإزالتها من السحابة مهمة موثقة تحتاج موافقة المستخدم.

## 5. قواعد الكود

- **التعليقات بالعربية** (لهجة عراقية) وتُستخدم فواصل `═══` لعناوين الأقسام؛ أضفها عند الحاجة لكن لا تزين الكود.
- **SQL:** دائماً parameterized عبر `q`/`one`/`tx` من `backend/src/db.js` — ممنوع بناء SQL بالدمج النصي.
- **التحقق من المدخلات:** نمط whitelist `allowed = [...]` ثم دالة `norm()` للأرقام العربية (٠-٩/۰-۹) مع رسالة خطأ عربية واضحة؛ لا تثق بأي قيمة من الـ client.
- **رسائل الأخطاء:** السيرفر يرد `{ error: 'رسالة عربية' }` مع كود HTTP صحيح؛ التطبيق يعرضها عبر `on ApiException catch (e)` → Toast/SnackBar بـ `e.message` (لا تعرض stack traces).
- **Flutter:** كل طلبات الشبكة تمر من `app/lib/core/api/api.dart` المركزي (لا توزع `http` مباشرة في الشاشات)؛ بنية folders: features + screens؛ الثوابت نصية عربية مباشرة.
- **التسمية:** ملفات snake_case؛ كلاسات Dart بـ PascalCase؛ دوال ومتغيرات camelCase.
- **الحساس النقدي (محفظة/نقاط/سداد):** كل التحديثات داخل معاملة `tx()` مع أقفال/شروط ذرّية وفحص صحة الرصيد.

## 6. قواعد الأمان (إلزامية)

- ممنوع كتابة أسرار أو مفاتيح في الكود (انظر `SECURITY_AUDIT.md` C3 — الـ fallback الحالي `'zaboon-horizon-secret-2026'` ثغرة معروفة: اعتمد على `process.env.JWT_SECRET` فقط).
- تحقق من كل مدخل (نوع، مدى، طول) قبل استخدامه — خاص بالأسعار والكميات والنسب (لا قيم سالبة/صفر خاطئة).
- لا تستخدم `Math.random()` لأي شيء أمني (OTP/رموز) — استخدم `crypto.randomInt`، وضع حد محاولات (lockout).
- ممنوع تخزين رموز أو بيانات حساسة في localStorage/SharedPreferences إذا أمكن — وإذا لزم، أشر للبديل.
- الملفات الثابتة للوحة الأدمن تُقدَّم كما هي: أي حقل من المستخدم (logo/art/cover/avatar/gradient...) يجب أن يمر عبر `esc()` — راجع C2 في التقرير.
- ممنوع إضافة نهاية مسارات/إعدادات تُظهر تفاصيل الأخطاء للمستخدم.
- لا تختبر سيناريوهات **كتابة** على قاعدة الإنتاج أبداً — استخدم المحلية (انظر القسم 4).
- عند أي تغيير يمس الأمان: راجع `SECURITY_AUDIT.md` وحدّثه عند إغلاق ثغرة.

## 7. الممنوعات الصريحة

1. **لا تكسر الموجود:** لا تحذف أو تعيد تسمية ملفات/مجلدات قائمة بدون موافقة (أخطاء سابقة سبّبتها شاشات محذوفة أكسرت الاختبارات).
2. **لا تمسح ولا تعدّل بيانات مباشرة في قاعدة الإنتاج** (أرصدة، متاجر، طلبات) بدون إذن صريح — ومعالجة البيانات تتم بـ SQL مُوثَّق وبعد نسخة احتياطية.
3. **لا تترك بيانات اختبار** على الإنتاج (منتجات تجريبية، طلبات، أرصدة وهمية) — نظّف ما تنشئه.
4. **لا تعيد هيكلة شاشات كبيرة عاملة** (مثل `vendor_shell.dart` — شاشة التاجر المدمجة) دون خطة وموافقة مسبقة.
5. **لا تضف أسراراً للـ git** ولا ترفع `.env` أو ملفات حساسة.
6. **لا تعطّل الحمايات الأمنية القائمة** (تشفير TLS بقاعدة السحابة، حذف `DROP TABLE` في bootstrap على السحابة، فحوصات `roles(...)`) إلا بطلب صريح.
7. **لا تخلق ملفات بناء/أرشيف عشوائية داخل الريبو** (APK يُبنى فقط عبر `publish.sh` أو `flutter build`).
8. **لا تعتمد على الافتراضات** — افحص الكود الفعلي قبل تعديله، وتأكد من السلوك بالتشغيل.

## 8. سلوك العمل

- **خطة أولاً:** قبل أي مهمة كبيرة، اعرض خطة مختصرة (الخطوات + الملفات المتأثرة) وانتظر الموافقة. **مهمة كبيرة =** تلمس أكثر من ملف واحد، أو أي شيء متعلق بقاعدة بيانات/مالية/أمان/نشر/حذف — أي تعديل بسيط (ملف واحد، غير حساس) نفّذه مباشرة بدون خطة.
- **قسم واعرض:** قسّم المهام الكبيرة لخطوات صغيرة؛ وخذ موافقة بين الخطوات الرئيسية للمهام الحساسة (مالية، قاعدة بيانات، نشر، حذف).
- **تحقق بالتشغيل:** بعد كل تغيير مهم، شغّل الكود وتأكد من خلوّه من الأخطاء (`node --check` للسيرفر، `flutter analyze` للتطبيق، تجربة الـ API/البناء) — لا تكتفِ بالكتابة.
- **لا تلمس ما لم يُطلب:** أي تغيير خارج نطاق المهمة (إصلاح جانبي، refactor، إضافة ميزة) يحتاج لفت انتباه المستخدم أولاً.
- **أبلغ بوضوح:** عند انتهاء المهمة، لخّص ما تغيّر، وكيف تحققت، وما المتبقي (مثل خطوات النشر).
