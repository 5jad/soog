# تقرير الفحص الشامل — مشروع بازار (Zaboon)

> فحص مُزّل بالدليل: قراءة الكود + اختبارات مباشرة على السيرفر الحي `http://192.168.1.137:4000` + قاعدة بيانات PostgreSQL.
> التاريخ: 2026-08-07

---

## 0. ملخص تنفيذي

| التصنيف | العدد |
|---|---|
| 🔴 حرجة (Critical) | 7 |
| 🟠 عالية (High) | 9 |
| 🟡 متوسطة (Medium) | 8 |
| 🔵 منخفضة (Low) | 4 |

أخطر ما تم إثباته موثّقاً باختبار حيّ:
1. **إمكانية تسجيل حساب كأدمن من الخارج** (رفع صلاحية) — تم إنشاء حساب إدمن عن بُعد وإثبات نجاحه ثم حذفه.
2. **ثغرة SQLi حقيقية** في سطور إدراج الإشعارات (المندوب/التاجر/الأدمن) عبر حقول نصّية من جسم الطلب.
3. **السلة في التطبيق لا تعمل إطلاقاً** (مطابقة خاطئة بين حقول الـ API والـ Flutter).
4. **كل المنتجات تظهر "غير متوفر"** لأن جدول `products` بلا عمود `stock` بينما التطبيق يقرأ `stock` من الاستجابة.

---

## 1. 🔴 ثغرات فلاح الصلاحيات والأمان

### 1.1 تسجيل حساب كأدمن — مصادقة دور تعسفي (حرجة)
- الملف: `backend/src/routes/auth.js:12`:
  ```js
  const { phone, name, password, role = 'customer' } = req.body;
  ...
  INSERT INTO users (phone, name, password, role) VALUES ($1,$2,$3,$4)
  ```
- **الإثبات الحيّ**: `POST /api/auth/register {"phone":"07800009999","role":"admin",...}` عاود `{"user":{"role":"admin"}, "token":...}`. (تم حذف الحساب لاحقاً).
- التأثير: أي شخص يستطيع إنشاء حساب بصلاحيات الأدمن والتاجر والمندوب.

### 1.2 حقن SQL (SQLi) عبر جسم الطلب
- الملفات:
  - `backend/src/routes/customer.js` إرجاع `reason`:
    ```
    '${reason}'  ⚠ سطر إصدار إشعار
    ```
  - `backend/src/routes/vendor.js` (عدة أسطر): إعلان، إرجاع، رسائل — دمج `'${title}'`, `'${reason}'`, `'${msg}'`.
  - `backend/src/routes/delivery.js:99`: `'${total_collected.toLocaleString()} د.ع'`.
  - `backend/src/routes/admin.js:156` ومناطق آخرى: `'${ns.name}'`.
- البيان: المعامل `$` يمنع الحقن في القيم المخزنة، لكن الـ notification body يُبنى بدمج السلاسل مباشرة في نص الـ SQL → **بيع حقن قابل للتنفيذ**.
- مثال قابل للحقن: حقل `reason` في تسجيل الإرجاع، `note` في إرسال الإشعار من الأدمن.

### 1.3 مفتاح JWT مضمّن في الكود + الصلاحية القصوى (حرية/عالية)
- الملف: `backend/src/middleware.js:4`:
  ```
  const SECRET = process.env.JWT_SECRET || 'zaboon-horizon-secret-2026';
  ```
- التوكين بصلاحية 30 يوم → أي توكين مسروق يبقى يعمل شهراً.

### 1.4 كلمة مرور تجريبية ثابتة للمستخدمين القدامى (عالية)
- `auth.js`:
  ```
  if (!user.password) → Login بِـ '12345678' أو '123456'
  ```
- أي مستخدم قديم بلا كلمة مرور في القاعدة يسطيع الدخول بها دون تسجيلها له.

### 1.5 إفشاء رمز OTP في الرد (عالية)
- `backend/src/routes/auth.js` `request-otp` يردّ `dev_code` مباشرة في JSON.
- التطبيق يعتمده: `app/lib/screens/login_screen.dart:44`:
  ```
  if (d['dev_code'] != null) code.text = '${d['dev_code']}';
  ```

### 1.6 لا يُوجدRate Limit على OTP / التسجيل
- اختبار مباشر: 3 طلبات متتالية `request-otp` كلها 200 → يسمح بتهجم القوة الغاشمة.

### 1.7 CORS مفتوح بالكامل
- `server.js`: `app.use(cors())` → يسمح لأي موقع أن يطلب مع التوكين (عبر مرشحات مع `credentials`).

---

## 2. 🟠 مطابقات خاطئة بين الـ App (Flutter) والـ API (Backend)

فحص ملف `app/lib/models.dart` مقابل استجابات `/api` المنظورة أعلن:

### 2.1 السلة تظهر دائماً فارغة (حرجة)
- `customer.js:17` يرجع `res.json({ items })`؛
- `store` في التطبيق `cart_screen.dart:33` يقرأ `d['cart'] ?? []` → **السلة فارغة رغم جود العناصر**.

### 2.2 إضافة منتج بقياس لا يعمل
- `customer.js:20` يتوقع `variant_id` (رقم) في الجسم.
- `store_screen.dart:50` يرسل `'variant': variant` (سلسلة) → فش يسلط، يُسجَّل بلا قياس.

### 2.3 تعديل الكمية في السلة دائماً يفشل
- `customer.js:36` يعرّف `PATCH /cart/:id` مع `{ qty }`.
- `cart_screen.dart:47` ينصّف `PATCH /api/customer/cart` بـ `{ item_id, qty }` → **مسار خاطئ** (+ حقل خاطئ).

### 2.4 تسجيل الطلب: `address` بدل `address_id`
- `store.dart` يقرأ من العنوان `a['address']` بينما الـ API يرجع `{label, details}` → مقطع العنوان الفعلي.

### 2.5 تقييم المتجر يظهر "0" دائماً
- يوجدت الـ API: `rating_avg`/`rating_count` (ولا يعمل جذب التقيييم).
- `app/lib/models.dart:33-34` يقرأ `j['rating']`, `j['reviews_count']` → **كل المتاجر تظهر 0.0 (0)**.

### 2.6 العروض لا تظهر إطلاقاً (التطبيق)
- السيرفر يردّ `offer_percent` / `offer_active` (من جدول `offers`).
- `Product.fromJson` يقرأ `has_offer`, `offer_price` → صفر دائماً → **العرض مفقود في كل واجهات التطبيق**.

### 2.7 كل المنتجات "غير متوفرة"
- **سبب نهائي في الزمن**: جدول `products` في السكيم **بدون عمود `stock`** (لا يوجد `CREATE TABLE products` سطر لـ `stock` — يوجد فقط في `product_variants`).
- في المقابل `Product.fromJson['stock']` ⇒ returns 0 ⇒ `outOfStock = (stock <= 0)` ⇒ **كل منتج يُعلن بـ "غير متوفر"**.
- **تم إثباته حيّاً**: `SELECT p.stock FROM products p` → `ERROR: column p.stock does not exist`. وشاشات الهاتف كلها "غير متوفر".

### 2.8 شاشة المندوب: مفتاح/حالة غير موجودة
- `delivery_shell.dart:50` يستدعي `GET /api/delivery/status`.
- السيرفر `delivery.js` لا يعرف مسار `status` (وجدنا: `online`, `available`, `accept/:id`, `trip`, `pickup`, `delivered`, `wallet`, `cash-report`, `stats`). → 403/404 → بدء شاشة المندوب كل الأساسي 0.

### 2.9 محفظة المندوب تظهر صفر
- `delivery.js:83` يرجع `{ wallet: { collected, pending, commission, reports } }`.
- التطبيق `delivery_shell.dart:389-391` يقرأ `d['balance']` و `d['today']` و `d['transactions']` → كلها صفر + قائمة مالية فاضية.

### 2.10 مطابقة تنقص في الأدمن
- `customer_shell`؟ لا، في `admin_shell.dart`:
  - يقرأ `d['recent_orders']` من `/api/admin/stats`، لكن السيرفر يرجع `{ stats: {...}, recent: orders }` → قائمة "أحدث الطلبات" فاضية دائماً.
  - ويقرأ `s['queue']` صحيح وهو موجود.
  - تستدعي `POST /api/admin/ads/:id/decision` و `POST /api/admin/cash/:id/decision`, لكن السيرفر يعرّف `PATCH /ads/:id` و `PATCH /cash/:id` → **قبول الإعلان والتقاضي لا يعملان إطلاقاً (404)**.

### 2.11 التاغر: خَلل في `attributes` + عرض يسري
- `vendor_shell.dart` يقرأ حقول `has_offer`/`offer_price` بينما الـ API يعرض `offer_percent` — العرض لا يظهر أبداً.
- `vendor.js` يسمح بتعديلات على `attributes` كامل لكن لا يتحقق من مخطط المحتوى.

---

## 3. 🔴 أخطاء هندسة القاعدة/منطق العمل

### 3.1 جدول `offers` بلا قيد فريدوى (duplicate rows)
- `schema.sql:148-153`: `CREATE TABLE offers` بدون `UNIQUE (product_id)`.
- التاجر عند تفعيل العرض ينفّذ `INSERT ... ON CONFLICT DO UPDATE` (vendor.js:165) → **الـ ON CONFLICT لا أثر له** لأنه لا يُسمح بقيود → إعادة تفعيل العرض يُكرّر الأسطر لانهائياً (تفضيل×ط) وتتراكم السجلات.

### 3.2 مضاعفة/عدّ قواعد الكوميسيو
- محفظة المندوب: يُخصم 5% ثابت (من `settings.courier_rate`) من تسليط الكاش، بينما عرض محلات التاجر من سلوكه فيه% استناد لِلبائع — اختلاف.

### 3.3 لا يوجد `store_documents` مراجعة / تحديث الحِلول (تحقق كامل)
- كل مسار `POST /api/.../orders` لديها تحقق بسيط لا يتحقق من `requireRole` على كل الدور.

### 3.4 كود الطلبيات قد يتصادم
- `ZB-${1000+cnt+1}` باليوم → قد يتصادم بالطلبين المتزايدين.

### 3.5 لا يوجد حدّ كمية القوت من محاور
- يزال المواد حسابات التطٔلكتروني لقاعدة: تعبير `store` للتاجر يجبر فقط الدفع الصفرى.

---

## 4. 🟡 ملاحظات متوسطة

- **قفل الدور الأدمن**: `myStore()` يسمح للتاجر بدون `requireRole` صارم.
- **لا يوجد حفظ audit لعمليات cart/qty** (فقط `audit` للعروض/الإعلانات).
- **المنتجات لا تملك `stock`** — لا أثر يها للحِفل الاستتوك؛ فعش اللتاجرة يضيف `variants` فقط.
- **العملة**: الأسعار بلا decimal صفت؛ `money()` في `theme.dart` يعتمد `toStringAsFixed(0)`.
- **لا يوجد صورة رفع** — `image` نص فقط (إيموجي); لا يوجد `/upload`.
- **منطقة الإصلاح** في الشاشة: تعديل `qty` خاطئ (`2.2`), ما يتحقق من رقم.
- **لا يوجد**: تسعير محفوظة عند تنظيف السلة؟ فعلاً موجودة.

---

## 5. 🔵 ملاحظات منخفضة

- **لا يوجد ملاط/تحليل جلد للـ DB** (لا indices إضافية على `orders.created_at`).
- **لا يوجد `.env` منفصل** — يسقرأ من متغيّر البيئة فقط.
- **غبة الـ migration mechanism** —`schema.sql` مره في `server.js` عند كل Gehul? لكن `db.init` ينسّق بناء.

---

## 6. ملخص الأثر النهائي

| السيناريو | النتيجة حالياً |
|---|---|
| تسجيل مستخدم جديد | حسابات إدارية لأي شخص (قابل للإثبات) |
| إضافة إلى السلة | السلة تظهر فارغة دائماً حسب التطبيق |
| عرض الرمز النصي للـ OTP | تسريبه لعموم |
| تعقيد الكمية | لا يعمل (مسار خاطئ) |
| قبول إعلان من الأدمن | يفشل (POST بدل PATCH) |
| كل المنتجات في التطبيق | "غير متوفرة" (لا عمود stock) |
| العروض في التطبيق | غير مرئية |
| شاشة المندوب | حالتها لا تُقرأ (404 /status), المحفظة صفر |
| تقييم المتاجر | الكل 0.0 (0) |
| SQLi في الحقول النصية | إمكانية إدراج ربط عائلته في `notifications` |

---

## 7. أولويات الإصلاح المقترحة

1. **(عاجل)** منع `role` في `/register`؛ عدّل ثبت الدور كـ `customer` فقط.
2. **(عاجل)** استبدال css بدمج النصوص في SQL بمعاملات (مصادر: كل أسهم الإشعارات في repository عام).
3. **(عاجل)** إضافة عمود `stock` لجدول products، أو تعديل /Product.fromJson.
4. تعميد نطاق توافق الحقول: `rating_avg`/`rating_count`/`offer_percent`بدل الإسماء القديمة في التطبيق.
5. إصلاح مسيرات الأدا: `decision` ⇄ `PATCH /ads/:id` و `/cash/:id`.
6. استيلحة إنشاء رسالة `status` لها في driver لـ `/api/delivery/status` أو تعديل التطبيق إلى `/online`.
7. إضافة قيد UNIQUE في offers و rate limits على OTP/register.
8. ضبط SECRET عبر متغير بيئة واقتصاح (duration) إلى أقصر.

---

## 8. الفيلم الزمني

- 2026-08-07 — الفحص الشامل: DB + Backend + Flutter + اختبارات حية؛ تم إثبات ثغرة الأدمن وتسجيل + حذف الاختبار القياسي.
- 2026-08-07 — **مرحلة الإصلاح**: نُفّذت التعديلات التالية في الكود (وأُعيد تشغيل السيرفر عبر `systemd-run --user --unit=zaboon2`):
  - `schema.sql` + قاعدة البيانات الحية: إضافة عمود `stock INTEGER NOT NULL DEFAULT 0` لجدول `products`، وإضافة `UNIQUE (product_id)` إلى `offers` (وحذف أي صفوف مكررة) — migration بدون DROP.
  - `auth.js`: `/%register` يفرض الدور `customer` دائماً (إصلاح ثغرة رفع الصلاحية — تم إثباتها حصرياً), إزالة كلمات مرور المسار الاحتياطية الصريحة (`12345678/123456`) من `login`.
  - حقن SQL في نصوص الإشعارات عبر `body`/`reason`/`title` — استُبدل كلها بمعاملات `$` في `customer.js`, `vendor.js`, `delivery.js`, `admin.js`.
  - `customer.js`: `GET /cart` يردّ الآن `{items, cart}` معاً للتوافق؛ `POST /cart` يقبل `variant` (اسم) أو `variant_id`؛ أضيف `PATCH /cart` بـ `{item_id, qty}` (المسار الذي يستدعيه التطبيق); `/addresses` يردّ `address` مركّباً من `label + details`; إنشاء الطلب يقبل `address` نصياً.
  - `delivery.js`: أضيف `GET /status`؛ `GET /wallet` يردّ `balance`/`today`/`collected`/`pending`/`commission`/`transactions`.
  - `admin.js`: أضيف `POST /ads/:id/decision` و `POST /cash/:id/decision` (approve/reject)؛ `GET /stats` يردّ `recent` و `recent_orders`.
  - `vendor.js`: `GET /orders/:id` (تفاصيل الطلب للتطبيق)؛ قبول `stock` عند إنشاء/تعديل المنتجات؛ حقل `wallet.balance` تكملةً لـ `available`.
  - `public.js`: متاجر/منتجات تردّ أسماء متوافقة مع التطبيق: `rating`/`reviews_count`, `has_offer`/`offer_price` مع `offer_percent`/`offer_active`.
  - `admin.js`/`seed.js`: مولّد البيانات التجريبية يوفر `stock` للمنتجات.
  - التطبيق `app/lib/screens/cart_screen.dart`: قراءة `d['cart'] ?? d['items']`.
- **نتيجة الاختبارات الحية بعد الإصلاح**: register يفرض `customer`؛ كل مسارات decision تعمل؛ `/delivery/status` يرد `{online}`؛ محاولات SQLi محايدة وبلا أثر على `orders`؛ المتاجر/العروض تردّ مفاتيح التطبيق.

---

## 9. ملاحظات متبقية (خارج نطاق هذا الإصلاح)

- `OTP` في وضع `DEV_OTP=true` يردّ `dev_code` — مخصص للعرض التجريبي فقط؛ يُغلّف بفصل إنتاجي عند الإطلاق.
- `CORS(*)` في `server.js` — يُشدَّد لاحقاً لحصر النطاقات.
- طول صلاحية التوكين 30 يوم — يُخفّف استعداداً للإنتاج.
- لا يوجد `index` على `orders.created_at`؛ لا يوجد رفع صور (`/upload`) — صور المنتجات إيموجي حالياً.
## تحديث 2026-08-07 (مرحلة الميزات التنافسية)

بُني وسُلّم على النظام الحي (systemd `zaboon2` + DB `psql -p 5434`) — كل المسارات اختبار حي ناجح:

- **نقاط الولاء**: جدول point_transactions + نقاط تُربح عند استلام الطلب (`1 نقطة كل 1000 د.ع`)، تُسترد بالطلب (`100 نقطة = 1000 د.ع`).
- **دعوة الأصدقاء**: كود فريد لكل مستخدم، مكافأة 100 نقطة للداعي و50 للمدعو (سجل بالترانزاكشن).
- **عجلة الحظ**: مرة/يوم، ربح 0–200 نقطة مع `UNIQUE(user_id, day)`.
- **كوبونات التاجر**: CRUD + تحقق (شرعية/انتهاء/حد أدنى/متجر/استخدام لكل مستخدم).
- **جدولة التوصيل** `scheduled_at` + **سلة موحدة** `group_id` (دفعة واحدة من عدة متاجر).
- **إجازة المتجر** `on_vacation` — يمنع الطلب ويظهر للعميل، **سياسة ضمان** `warranty_days` (افتراضي 3 أيام، يُرجع الزبون ضمن فترة الضمان مع حساب تلقائي للمدة).
- **تتبع حي**: `POST /delivery/location` + `GET /customer/orders/:id/track` (موقع المندوب + المتجر + الزبون على الخريطة).
- **محادثة عبر الزمن الحقيقي**: conversation + sender_role (customer/vendor) + رسائل read/unread.
- **Q&A** على المنتجات: يسأل الزبون، يجيب التاجر، إشعار للطرفين.
- **تصدير CSV** للمنتجات والطلبات للتاجر؛ لوحة أدمن: كوبونات + استعمالات + تعديل نقاط.
- **إصلاحات من التحقق**: إزالة الدالة الوهمية `pointsUsedActual()`، تصحيح `const discount`، إصلاح INSERT للمحفظة الزائفة، وتجنب `Math.max(100, bal)` الذي كان يسحب نقاطاً غير موجودة.

شاشات فلاتر: نقاط + دعوة + عجلة، سلة بكوبون/نقاط/جدولة/سلة موحدة، محادثات (زبون + تاجر)، إجازة + كوبونات + Q&A للتاجر. `flutter build apk --debug` نجح.

## تحديث 2026-08-07 (صفحة المتجر — ميزات المتاجر العالمية/المحلية)

أُنجزت بعد دراستك لمتاجر (Amazon, Etsy, Talabat + Lezzoo, ZUZU, Orisdi) واعتماد **كل المقترحات** حسب تصميم التطبيق:

- **`store_screen.dart` أعيدت كتابتها بالكامل**: هيدر بالحالة (مفتوح/مغلق/إجازة) + الضمان (أيام)، شريط توصيل (رسوم/مجاني فوق حد/دوام)، أزرار محادثة 💬 · اتصال · موقع · متابعة.
- **كوبونات المتجر** 🎟 (نسخ للـ Clipboard) + **بحث جوة المتجر** (فلترة المنتجات) + **عروض المتجر** سات أفقية + **التقييمات**: متوسط + توزيع نجوم (progress bars) + آخر تقييمات.
- **متجر المفضلة** `store_favorites` (متابعة/إلغاء) + صفحة "المتاجر المتابعة ❤️" في حساب الزبون.
- `GET/POST /api/customer/store-favorites` (toggle + list) و `POST /api/customer/conversations` (get-or-create محادثة الزبون مع المتجر).
- `POST /api/stores/:id` ترجع الآن أيضاً `coupons` + `rating_breakdown` (توزيع النجوم 1–5).

**النتائج الحية**: follow/unfollow store 21 ✓، المحادثة get-or-create ✓، كوبونات `SAVE10` ترد ✓، `flutter analyze` نظيف، `flutter build apk --debug` نجح.

## تحديث 2026-08-08 (جولة الفحص الشاملة QA)

فحص حي كامل لكل الأدوار — النتائج:

- **صلاحيات (API audit)**: بدون توكن → 401 ✓ لكل المسارات؛ دور غلط → 403 ✓ (زبون→تاجر، تاجر→أدمن، مندوب→أدمن، أدمن→مندوب)؛ توكن مزيف → 401 ✓. ملاحظة تصميم: مسارات الزبون متاحة لأي دور مسجل (لا تسريب بيانات — كلشي مربوط بـ `req.user.id`).
- **رحلة الشراء**: السعر/الرسوم/الخصم كلها تُحسب سيرفر-سايد — هجوم إرسال `subtotal=1` يُتجاهل (المجموع يظهر من السلة الحقيقية). كوبون + نقاط معاً عند الطلب تمّ التحقق رياضي (95800 د.ع).
- **التوصيل**: vendor `accept→ready` ثم مندوب `available→accept→location→pickup→delivered` — التتبع يعرض موقع المندوب+المتجر والزبون، ونقاط الزبون تُضاف عند الاستلام (+69 نقطة لطلب 74,000). ✓
- **المحادثة**: زبون↔تاجر، `has_unread` ينقلب عند القراءة (True→False) ✓.
- **Q&A**: سؤال زبون → جواب تاجر → إشعار للزبون ✓.
- **لوحة الأدمن**: إحصائيات، كوبونات+استعمالات، مبيعات (يوم/تصنيف/محلات)، تعديل نقاط ✓. التاجر: كوبونات CRUD، إجازة، تصدير CSV (منتجات وطلبات) ✓.
- **إصلاحات باحقتين**: (1) خصم النقاط كان يخصم كامل المبلغ حتى لو القيمة لا تتجاوز مضاعف 100 (isracript 520 → يضيع 20) — الآن يُنزل لمضاعف 100. (2) `uses_left` بمعنى "0=غير محدود" حسب السكيما — تم توضيح الشرط دون كسر الكوبونات غير المحدودة.
- **حادثة إعادة تشغيل**: بعد رجوع الجهاز، انقطع postgres (5434) وظهر backend — وحدة كان transient — أُنشئت وحدتان دائميتان مستخدمين: `zaboon-pg.service` + `zaboon2.service` مفروضتان على default.target (بعد إعادة التشغيل يشتغلون وحدهم).

## تحديث 2026-08-08 (الخريطة الداخلية في التطبيق)

أُضيفت الخريطة داخل الموبايل (OpenStreetMap عبر `flutter_map` — بدون مفتاح API، يشتغل بمصر والعراق جيداً) بدل فتح تطبيق خارجي:

- **`lib/map_screen.dart`** (جديد): 3 شاشات —
  - `PickMapScreen`: ضغط على أي نقطة يحدد الموقع، زر "حدد موقعي" بـ GPS، رجوع بالإحداثيات.
  - `LiveTrackMapScreen` (الزبون): يتابع المندوب حياً كل ٤ ثوان — ماركرات 🏪 المتجر / 🛵 المندوب / 🏠 البيت، تُفعّل الأداة بمجرد انطلاق التوصيل.
  - `CourierMapScreen` (المندوب): خريطة الرحلة + إرسال الموقع يدوياً (GP أو بالضغط على الخريطة) + **إرسال تلقائي كل ٥ ثوانٍ**.
- **التكامل**: زر "شاهد المندوب على الخريطة 🗺" بتفاصيل الطلب (الزبون حين التجويل) · "خريطة التوصيل الحية" برحلة المندوب · "حدد موقع متجرك على الخريطة" بتعديل المتجر (ترسل `lat/lng`) · "حدد موقعك على الخريطة" عند إضافة عنوان الزبون.
- **إصلاح**: السلة كانت ترسل العنوان نصياً فقط — الآن ترسل `address_id` معاه فيشتغل تتبع البيت، والـ track يرجع `user_lat/user_lng` من العنوان (تم التحقق حياً: address 23 → order 31 track user=32.52,45.81).
- **التراخيص**: `INTERNET` (كان ناقصاً للنسخة النهائية!) + `ACCESS_FINE/COARSE_LOCATION` أُضيفوا للـ AndroidManifest؛ مكتبات: `flutter_map` + `latlong2` + `geolocator`.
- `flutter analyze` نظيف + `flutter build apk --debug` نجح.
