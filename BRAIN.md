# 🧠 BRAIN — عقل مشروع «زبون» الكوت

> هذا الملف هو المرجع الأعلى للمشروع. **أي تعديل/قرار/إصلاح/تحديث يُنفَّذ في المشروع يجب أن يُوثَّق هنا فوراً** (قسم «سجل التحديثات» آخر الملف).
> قاعدة ذهبية واحدة: الملف يبقى دائماً صادقاً مع الحالة الآنية للكود والجهاز.

---

## 1) ما هو المشروع

منصة تسوق وتوصيل محلية لمحافظة واسط (الكوت) متكاملة بـ 4 أدوار، اسم المنتج «زبون»:

| المكوّن | التقنية | المسار |
|---|---|---|
| التطبيق | Flutter (Android) — هوية زجاجية | `app/` |
| الباك اند | Node.js + Express 5 + PostgreSQL | `backend/` |
| لوحة تحكم | HTML/JS نقي RTL (نفس السيرفر) | `admin-dashboard/` |
| موقع تعريفي + تحميل APK | HTML نقي | `landing/` |
| ديمو تصميم (مرجع) | HTML | `app-pages-demo.html` + `design-system-demo.html` (في الجذر) |
| قاعدة بيانات محلية | PostgreSQL **منفذ 5434** (معزولة، لا sudo) | `infra/` |

- الدفع: **كاش عند الاستلام فقط** 💵 (لا بوابات دفع).
- التوسعة: المحافظات/الأحياء تُدار من اللوحة، والتطبيق يتسلمها مباشرة (`/api/governorates`).
- مبدأ الهوية التصميمية: **«المال صلب، الجمال زجاج»** — أزرق محيطي `#1D4ED8`، تدرجات ≠ وابيض دافئ `#FAF9F6`.

---

## 2) التشغيل اليومي (أوامر معتمدة)

```bash
# القاعدة (مرة واحدة فقط عند أول تشغيل):
cd infra && initdb -D pgdata -U postgres --auth=trust
cd infra && pg_ctl -D pgdata -o "-p 5434 -k /tmp" -l pg.log start

# الباك اند:
cd backend && npm install && npm run db:init && npm run db:seed && npm run dev
#   → http://localhost:4000  (حوّل بسيط: /api/health — سير يعمل)

# تطبيق Flutter:
cd app && flutter pub get
flutter analyze          # المطلوب دائماً: 0 أخطاء
flutter build apk --debug
#   → build/app/outputs/flutter-apk/app-debug.apk
```

**حسابات التجربة (seed):**
| الدور | الهاتف | الدخول |
|---|---|---|
| أدمن | 07900000000 | `admin123` |
| زبون | 07731234567 | OTP معروض في رد الـ API (DEV_OTP=true) |
| تاجر (الأصيل) | 07701112233 | OTP |
| مندوب (حسين) | 07705556677 | OTP |

> ⚠️ حساب الجلسة الفعلي المستخدم على الجهاز: `000000000555` / `pass1234` (زبون عنده سلة نشطة ونقاط).

---

## 3) الباك اند

### 3.1 الإعدادات
- `backend/.env` → `PORT=4000`، `DEV_OTP=true` (يطبع الرمز بـ console).
- وسيط `middleware.js`: `auth` (JWT) + `roles(...)` + `signToken/publicUser`.
- Socket.io: غرفة لكل دور (`join`)، إشارات: `order:new` → تاجر، `order:ready` → مندوب، `order:update` → الجميع.

### 3.2 المسارات (نقاط النهاية الحيوية)

**عام (public):**
- `GET /api/health` — فحص الحياة
- `GET /api/governorates` (مع districts مضمّنة)
- `GET /api/categories` (مع `attrs`)
- `GET /api/stores?district_id&category_id&governorate_id&q` — **approved فقط**، مرتب حسب التقييم
- `GET /api/stores/:id` — محل + منتجات + عروض + تقييمات + كوبونات + توزيع النجوم؛ متجر demo يُخفى
- `GET /api/products?store_id&category_id&q&offer&best`
- `GET /api/products/:id` — مع variants
- `GET /api/ads` ، `GET /api/offers` ، `GET /api/settings`

**المصادقة (auth):**
- `POST /api/auth/register` (دور **customer ثابت دائماً** — ثغرة رفع الصلاحية مسدودة + نقاط دعوة: 100 للداعي / 50 للمدعو) — يتطلب رمز OTP مؤكد للهاتف (يُرسل عبر `request-otp` قبل التسجيل)
- `POST /api/auth/login` (بكلمة مرور فقط لحساب كلمته مسجلة)
- `POST /api/auth/request-otp` (OTP للمستخدم **الجديد فقط** — يرفض الأرقام المسجلة مسبقاً؛ يُوصّل عبر تليجرام أو SMS)
- `GET /me` ، `PATCH /profile` ، `GET /search` (أدمن فقط)

**الزبون (customer):** سلة (GET/POST/PATCH-DELETE مع `item_id,qty` و `variant_id`)، عنوان، مفضلة منتجات + متابعة متاجر، طلب (منفرد + مجموعة بالـ `group_id`)، تفاصيل طلب، كوبون، نقاط (تسخير/السحب/العجلة)، QA، مراجعات، محادثات، إشعارات.

**التاجر (vendor):** الملف، المنتجات (CRUD + variants + attributes)، العروض، الطلبات (قبول/جاهز)، استرداد، المحفظة (أرباح/سحب)، كوبونات المتجر، الأسئلة، دردشة.

**المندوب (delivery):** حجوزات طلبات جاهزة، قبول رحلة، تحديث تتبع (lat/lng + مسار)، تسليم، تقرير كاش، أسعار/يرسة.

**آدمن (admin):** محافظات/أحياء، أقسام/ميزات، متاجر + مستندات + قرارات، منتجات، تحديدات، نماذج، كوبونات، إعلانات + باقات + قرارات، محفظة/سحب، مستخدمون (block/role)، إحصائيات (dashboard)، سجل تدقيق، إشعارات، رموز أدمن.

### 3.3 قاعدة البيانات (`backend/src/schema.sql` — 449 سطراً)
- **المحافظات/الأحياء**: `governorates` ← `districts`
- **المستخدمون**: `users` (نقاط/مرجع)، `otp_codes`، `point_transactions`
- **التصنيف**: `categories` ← `category_attrs` (خصائص المنتجات)
- **المتاجر**: `stores` (حالة CHECK: `pending|approved|rejected|suspended`، عمولة 10%)، `store_documents`، `wallets` + `wallet_transactions`
- **المنتجات**: `products` (سمات JSONB) ← `product_variants`، `offers`
- **المتاجرة**: `cart_items` (UNIQUE user+product+variant)، `coupons` (+`coupon_usages`)، `store_favorites`، `favorites`
- **الطلبات**: `orders` (حالة CHECK: `new|preparing|ready|delivering|delivered|cancelled|returned`، `group_id` للتسوق المتعدد)، `order_items`، `order_status_history`، `delivery_trips` + `trip_orders` + `delivery_track_log`
- **التواصل**: `conversations` + `messages`، `product_questions`
- **الولاء**: `reviews`، `refund_requests`، `spin_wins` (مرة يومياً لكل مستخدم)
- **الإعلانات**: `ad_packages` + `ad_requests`
- **النظام**: `notifications`، `settings`، `audit_logs`

> وحدة التيم المهمة: `orders.discount` تكوّن الكوبون + النقاط معاً؛ حقول `points_used/points_discount/points_earned` تقارن بـ `total`.

---

## 4) تطبيق Flutter

### 4.1 الأساسات
- `api.dart` — عميل HTTP الوحيد: `Api.base = http://192.168.1.137:4000` (**IP جهاز التطوير الحالي — يُغيَّر عند نقل الجهاز**)؛ كل طلب مع `Authorization: Bearer`.
  - `Api.load()` في `main.dart` قبل `runApp` — إذا انتهى التوكن يـ `clear()`.
  - `AppState.i` — حالة مشتركة: `cartCount` (ValueNotifier)، `guestCart`، عداد إشعارات.
- `theme.dart` — هوية «زبون» v2 (قرار الدراسة 2026-08-11): `A.primary=12294E`، `A.accent=F2560F` (CTA شراء حصري)، `A.card()` صلب، `A.glass()` للشيتات فقط، شبكة 8pt، Tajawal، `IconGlass`، `ChipG`، `GlassCard`، `SolidBtn` (52px)، `SheetTitle`…
- `widgets.dart` — `GlassBottomNav` (شريط سفلي موحّد، شارات عبر `badgeIndex`/`badgeCount`)، عنصر زر عائم، زر دفع عادي… — *مهم جداً*: ممنوع أي `margin` سالب (يوجب red screen في debug).

### 4.2 4 واجهات بالأدوار (`shell.dart` يوجّه):
- **زبون** `customer_shell.dart` — 4 تبويبات: الرئيسية + المتاجر + السلة + طلباتي + حسابي (5 فعلياً)، `IndexedStack`. الأقسام القواعد life: أعلى الشاشة (تحديث في **منتصف** الصفحة — قرار الجلسة).
- **تاجر** `vendor_shell.dart` — إدارة المحل، إعداد، الطلبات، نقاط، دردشة.
- **مندوب** `delivery_shell.dart` — الطلبات الجاهزة + خريطة الطريق + التتبع الحي.
- **أدمن** `admin_shell.dart` — إحصائيات، المتاجر، الإعلانات، المحافظ…

### 4.3 شاشات زبون مفصّلة
- `stores_screen.dart` — شبكة متاجر + تصفية (قسم/حي/بحث).
- `store_screen.dart` — دكان واحد: هيرو + عروض + منتجات + تبويب تقييمات/أسئلة.
- `category_products_screen.dart` — منتجات قسم خاص.
- `cart_screen.dart` ⭐ (الأكثر إعادة بناء) — **سلة متعددة المتاجر**:
  - `embedded: true` → يعطى `Column` (رأس + قائمة + شريط سفلي) (لا Scaffold — هكذا تحل مشكلة الظهور داخل التبويب).
  - بطاقة لكل متجر (شعار/توصيل/mجموّع فرعي) + بطاقات أصناف مع عدّاد.
  - جدول التوصيل **في نهاية القائمة** (تحت كل المتاجر).
  - **شريط سفلي زجاجي**: حقل كوبون «🎟️» + زر «تطبيق» كبير (100×47) يُطبَّق على **إجمالي السلة الكامل** ← `_applyCoupon(groupList.first['store_id'], total)`؛ سطر النقاط (Switch نقداً)؛ ثم «الإجمالي + عدد الأصناف + دفع كاش 💵 + زر «إتمام الطلب ✓» → `placeGroupOrder(groupList)` (طلب مجموعة واحد لكل المتاجر).
  - لا أزرار طلب منفردة — واحد فقط.
- `orders_screen.dart` — قائمة + تفاصيل + تتبع + مراجعة + نقاط.
- `account_screen.dart` — حسابي + تبديل الأدوار (تُجع processed عبر `rolesOf`).
- `points_screen.dart`، `notifications_screen.dart`، `chat_screen.dart` (محادثة زبون⇔تاجر؛ تُفتح بـ `ChatListScreen(role:...)`)، `login_screen.dart` (OTP + بورد)، `map_screen.dart` (998 سطر — خريطة المسار/عدّاد).

### 4.4 الهوية البصرية
- ألوان (`A.`): primary `1D4ED8`، cyan `06B6D4`، accent (برتقالي) `F97316`، موفق خلفية `FAF9F6`، زجاجية (gradient شفافي 0.62/0.74 + حافة بيضاء + ظل أزرق).
- الخطوط: `fontWeight` عالي (700/900)، نص شرقي بالطّرف؛ «Ĝlass» كل بطاقاتها.

---

## 5) اللوحات الثانوية
- `admin-dashboard/` — لوحة ويب HEA (HTML/JS نقي) في `/admin` على نفس السيرفر؛ «وجّه أقل مهارات»: 7 أقسام: نظرة عامة، المتاجر، الطلبات، المحتائج، الإعلانات، المستخدمين، الإعدادات + دخول بأدمن (بورد/password mode).
- `landing/` — صفحة حمل «زبون» + `GET /download` يسحب `zaboon-app.apk` من `landing/downloads/`.
- `app-pages-demo.html` + `design-system-demo.html` — **ديمو مرجعي تصميمي** (واجهات التطبيق + ملف الألوان) في الجذر؛ ليست جزءاً من البناء الفعلي.

---

## 6) سجل القرارات والجلسات (تراكمي — يحدَّث فوراً)

| التاريخ | القرار/الحدث | التفاصيل |
|---|---|---|
| جلسات سابقة | بنية المنصة الثلاثية + دعم 4 أدوار + دبل fences | سلة متعددة المتاجر بـ `group_id` ونقطة إجمالي |
| 2026-08-07 | إصلاحات تهيئة وإشعارات ومحادثات ودفع | AUDIT_REPORT.md مفصل بجذره |
| 2026-08-08+ | **الشاشة الحمراء**: السبب `margin: EdgeInsets.only(top:-26)` في `widgets.dart` (زر سلة بارز) — المخالف لقيود `isNonNegative` → استُبدل بـ `Transform.translate`؛ لا red screen من التطبيق بعدها | بقايا وميض = خاصية screen-mirroring في الجهاز (Miracast) لا التطبيق |
| 2026-08-08 | **تبويب السلة**: `CartScreen(embedded:true)` داخل `IndexedStack` — كانت تظهر فارغة لأن الأعمدة خارج Scaffold; الحل: عودة `Column` في وضع embedded |
| 2026-08-08 | **تصميم السلة النهائي**: بطاقات متجر + عدادات، الكوبون/النقاط **للشحنة كلها** داخل شريط السعر (ليس لكل متجر)، جدول التوصيل أسفل، زر طلب واحد «إتمام الطلب ✓» |
| 2026-08-08 | **خط الأفقي الرسومي**: إضافة زر محادثة في المريط العلوي `customer_shell` (أيقونة فقاعة) يفتح `ChatListScreen(role:'customer')` |
| 2026-08-09 | **قياس عيادة السلة**: طرف إعادة بناء → هدف `y=190..236` (كما «المتاجر») — حالياً العنوان في `y=56..102` (أعلى بفاصل ~134)؛ **جارٍ حل الإزاحة** |
| 2026-08-09 | **تصميم Shein (جلسة)** — بطاقات المنتجات: صورة مربعة 1:1 بملء الحافة، حواف **حادة (radius 0)**، شبكات **متلاصقة (0 spacing) وملامسة لأطراف الشاشة**، كتلة مضغوطة (اسم 11.5 + سعر accent 13.5 + زر +)، نفس التصميم في: الرئيسية/صفحة المحل/المتاجر/الأقسام. سلايدر إعلانات **تلقائي (Timer 4ث)** مع نقاط مؤشر متحركة. «محلات مميزة»: شريط تدرج لوني + شعار أبيض مضغوط + تقييم نجمي (بدل القديم) |
| 2026-08-09 | **إنشاء BRAIN.md** — هذا الملف ”عقل المشروع“ بعد فحص شامل |
| 2026-08-09 | **صفحة المنتج (Shein)** — صورة مربعة بكامل العرض + شارة «خصم % 🔥» + سعر كبير (قديم مشطوب) + بوكس المتجر (شعار/اسم/موثق/تقييم/زر المتجر) + متغيرات + بطاقة توصيل/كاش/ضمان + وصف + تفاصيل + **التقييمات** (نجوم + تعليقات) + **منتجات ذات صلة** (نفس القسم) + **منتجات من نفس المحل** (أفقي scroll) + زر **مشاركة** (نسخ رابط) و**مفضلة** بجانب زر «أضف للسلة» في الشريط السفلي |
| 2026-08-09 | **حساب التست الجديد** (بعد إعادة بذر DB): `07750009999 / test1234` (customer مسجل) — حل محل `000000000555/pass1234` في دورة إعادة التوكن |
| 2026-08-09 | **التعليقات تظهر الآن**: البذرة ما كانت تضيف أي reviews — أضفت 11 تعليق عربي (5 نجوم + تعليق + أسماء) على 4 متاجر في seed.js |
| 2026-08-09 | **زر المشاركة**: استبدال نسخ الرابط بـ share_plus 10.1.4 (13.3.0 كسر بناء Kotlin → أنزلت) — يفتح نافذة اختيار Android |
| 2026-08-09 | **صور متعددة للمنتج**: عمود `images TEXT[]` + رفع صور من معرض الجهاز عبر `POST /api/uploads/upload` (JSON base64 — multer سبّب قطع RST غامض) → ملف في `backend/public/uploads` يُخدَّم بـ `/uploads/*`. واجهة التاجر: منطقة «صور المنتج (8)» بمعاينة + حذف + زر «أضف صور» (image_picker). صفحة المنتج: سلايدر PageView + نقاط مؤشر (الانتقاد: الدوائر/الشارة فوق الصورة) |
| 2026-08-09 | **باسوردات الديمو بإعادة البذر**: التاجر/المندوب = `vendor123` — (القبل كانت OTP–only); التبديل بالأدوار |
| 2026-08-11 | **دراسة تصميم «زبون» v2 (قرار نهائي)** — بيانات: Ipsos العراق 2025 (21% تسوق أونلاين، 82% سوشل ميديا، 91% جنوب العراق = سوشل كومرس)، Baymard (70% هجر سلة)، سيكولوجية لون 2026 (كحلي+أبيض+برتقالي أفضل تحويل، برتقالي CTA +32.5%)، Apple HIG 8pt/44pt، Fitts/إبهام. **المقرَّر**: كحلي ليلي `#12294E` + برتقالي CTA وحيد `#F2560F` (60-30-10) + أبيض دافئ `#FAFAF7` + Tajawal + شبكة 8pt حصرية (4..96) + زوايا 12/16/20/pill + زجاج للشيتات فقط (البطاقات اليومية صلبة) + أزرار شراء 52px مثبتة أسفل. حذف اسم «أفق» نهائياً |
| 2026-08-11 | **تنفيذ هوية v2 في التطبيق**: `theme.dart` أُعيدت كتابته (ثوابت A الجديدة + BuildTheme Tajawal)، `widgets.dart` (SolidBtn 52/capsule، GlassBottomNav صلب بلا blur، GlassCard صلب)، إزالة كل `A.glass` من البطاقات اليومية (customer_shell/stores/account/vendor/favorites/notifications/category/cart)، أزرار الشراء (أضف للسلة/إتمام الطلب ×3) صارت `A.accent` برتقالي. `flutter analyze` 0 أخطاء + `flutter build apk --debug` ✓ |
| 2026-08-11 | **توحيد الاسم + الهوية خارج التطبيق**: `admin-dashboard/` و`backend/public/admin` CSS vars → v2، `landing/index.html` كامل الألوان → v2 (Tajawal، hero كحلي، CTA برتقالي، header صلب)، ديمو HTML (design-system + app-pages) نُقلت ألوانها لنظام v2 (متبقي: إعادة توليد بنيوية كاملة للديمو الزجاجي القديم)، `PROJECT/BRAIN/README/schema.sql` بلا «أفق» |

---

## 7) دورة العمل اليومية مع الجهاز (Honor — ستدورها مع كل جزلة)

1. `flutter analyze` → **0 أخطاء** (warnings مقبولة/مثبّتة).
2. `flutter build apk --debug` → `adb install -r .../app-debug.apk` (Success).
3. **إعادة التوكن** بعد كل تثبيت (مهم): login عبر `curl http://localhost:4000/api/auth/login` بحساب `07750009999/test1234` (مسجل للتست — `000000000555` لم يعد يعمل بعد إعادة بذر DB) ثم كتابة التوكن في `shared_prefs` باسم التطبيق `com.zaboon.zaboon` (FlutterSharedPreferences.xml — مفتاح `flutter.zaboon_token`).
   - حسابات الديمو (بعد إعادة البذر): أدمن `07900000000/admin123` · تاجر الأصيل `07701112233/vendor123` · بوتيك النور `07702223344/vendor123` · طفولة `07703334455/vendor123` · مندوب `07705556677/vendor123` · زبون البذرة `07731234567` (OTP — إرم dev_code يظهر بالتوست).
4. `am force-stop` ثم `am start` وانتظار **~20s**.
5. لمسات المتبادلة (إحداثيات شاشة ثابتة):
   - تبويب المتاجر: `input tap 840 2590`
   - تبويب السلة: `input tap 600 2590`
6. **uiautomator dump لم يعد يرد نصوص** (شجرةفارغة) — القياس الوحيد الموثوق: `screencap` + فحص **بكسلي** (أعمدة داكنة بصفر أزرق) للنصوص في منطقة الصفحة.
7. لا تلمس جهاز المستخدم دون إذنه (الوضع: device مفتوح للزبون).

---

## 8) عيوب وملاحظات مفتوحة (من فحص 2026-08-09 — لم تُيَعْتَدَّ بعد)

### التناقضات:
1. ~~هويّة الأسماء~~ ✅ (2026-08-11): أُنجز توحيد الاسم مع دراسة الهوية الجديدة — «أفق» حُذفت من الكود/الوثائق (ديمو HTML تحتاج إعادة توليد كاملة للهوية v2 الصلبة — لونياً نُقلت، بنيوياً بقي الديمو الزجاجي القديم).
2. `app/lib/models.dart:40` — `Store.open` يقبل `'active'` في حين سكيما المتاجر لا تعرف هذه القيمة (الفحص `CHECK` يقبل `pending|approved|rejected|suspended`) — تسامح فارغ.

### التكرار:
3. **نقطتا نهاية متطابقتان**: `delivery.js` `GET /status` vs `GET /online` (نفس الاستجابة)؛ و`customer.js` `PATCH /cart` vs `PATCH /cart/:id` (نفس العملية بمسارين) — حذف المستغنى عنهما يُبسّط.
4. `admin-dashboard/js/api.js:16` — شرط إعادة التوجيه يستدعي `'/api/auth/verify'` **غير موجود** في الباك اند — شرط ميّت (التحقق الحقيقي بـ `/me`).

### أخطاء محتملة (وظيفية):
4. `admin.js:135-141` — تعيير `0` إلىdefault 2000 عند `delivery_fee`/`free_delivery_min=0` — يمنع القيمة الصحيحة «0».
5. `admin.js:157-159` — عند رفض المتجر يحدث الإشعار فقط لحالة `rejected` ولا يُعوّي `suspended`.
6. `vendor.js` و`admin.js` (مستندات) — تحديث `status` **بدون قيمة CHECK** في السكيما باسع: Bần ممكن → خطأ 500.
7. `delivery.js` تقرير كاش — `total_collected` يُستقبل من المتدون بدون إثبات مقابل مبالغ طلبات اليوم → توقيع بأي رقم.
8. `admin-dashboard/js/views/overview.js:41` — `commission_today` ثابت 10% ويغفل `commission_rate` في الإعدادات.

### تكرارات وثائقية:
9. ثلاث صفحات demo (الجذر `app-pages-demo.html` + `design-system-demo.html` + `landing/`) — مرجعية تصميمية فقط.
10. `test.html` (6 أسطر) و `test.js` (39 سطر) في الجذر — ملفات تجريب مهملة.

> توضيح: ملف `AUDIT_REPORT.md` (جذر) = سجل تدقيق سابق — يُقرأ ولا يُعتبر مرجعاً حيّاً (يُستبدل بهذا الملف).

---

## 9) تذكيرات صارمة (قواعد العمل الحالية)

- **لا `margin` سالب** في أي خطل (يمين القاعدة).
- **لا أزرار طلب متفرقة** في السلة — زر واحد.
- **التوثيق الضماني**: كل تعديل جديد → إضافة سطر في جدول «سجل القرارات» ✅.
- كل flag يضتبخvalent في التطبيق: عنوان مثل «إتمام الطلب ✓» جسم بصالة؛ لا اسـتك.
- ممنوع كشف أسرار في الأكواد المرفقة (env.dev هم amok).
- الاتصالات بالباك: `localhost:4000` من الجهاز عبر `http://192.168.1.137:4000` (نِسخة الجهاز الحالي جوهري).

---

_التحديثات تُضاف أدناه، الأحدث في الأعلى._