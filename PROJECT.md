# زبون — الكوت 🗺
### ملف تعريف المشروع الكامل (مرجع واحد لـ Claude AI)

> هذا الملف يمثل المشروع كاملاً: الفكرة، البنية، قاعدة البيانات، التصميم، والواجهات.
> اقرأه كاملاً، وسأجيب على أي سؤال عن أي جزئية منه أو أطور عليه.

---

## ١) الفكرة 💡

**منصة عرض وتوصيل متاجر محافظة واسط (الكوت)** — سوق إلكتروني محلي يجمع كل متاجر المحافظة في تطبيق واحد، ويوصل الطلبات عبر مندوبين تابعين للمنصة.

- النموذج: **منصة (Marketplace)** — المتاجر تعرض منتجاتها بنفسها، الزبون يطلب، مندوب يوصّل.
- الدفع: **كاش عند الاستلام فقط 💵** (لا بوابات دفع).
- التوسعة: المحافظات والأحياء تُضاف من لوحة التحكم — التطبيق يشتغل بها مباشرة دون تغيير كود.

### الأدوار (4)
| الدور | ماذا يفعل |
|---|---|
| 🧍 زبون | تصفح المتاجر/الأقسام، تركيبات المنتجات، سلة، طلب، تتبع، مراجعة، نقاط، محفظة، شحن رصيد |
| 🏪 تاجر | إدارة متجره ومنتجاته وتركيباته، قبول/رفض الطلبات، تقارير كاش |
| 🛵 مندوب | رحلات توصيل، تحديث حالة الطلب، تتبع مسار |
| 🛠 آدمن | لوحة ويب: محافظات/أحياء/أقسام/متاجر، إعلانات، كوبونات، راويت الشكاوى |

### المميزات
- السلة، الطلبات مع حالة كاملة (طلب → قبول → تجهيز → جاهز → توصيل → تسليم)، تتبع لحظي
- **نقاط ومحفظة**: نقاط تكافؤ تُحوّل لمحفظة تصرف بها للطلبات
- **عجلة حظ** (Spin)، كوبونات، شحن رصيد رفيع
- لوحة آدمن ويب + صفحة هبوط (Landing) + ملف APK يُحمَّل عند الزبائن

---

## ٢) التقنيات والبنية 🏗

```
project2/
├── backend/              Node.js + Express 5 + PostgreSQL (الجذر)
│   └── src/
│       ├── server.js     مدخل السيرفر: 4000 + رفع الـ APK
│       ├── schema.sql    36 جدول (PostgreSQL)
│       ├── db.js         استعلامات مساعدة (q, one, tx)
│       ├── migrate.js    ترحيلات يدوية (argv-column بجدول جدول)
│       ├── seed.js       بيانات الكوت: واسط+بغداد، أحياء، 10 أقسام، 5 متاجر، حسابات OTP
│       ├── routes/       auth · public · customer · vendor · delivery · admin · routing · uploads
│       └── middleware.js  auth (JWT Bearer)
├── app/                  تطبيق Flutter (Android)
│   └── lib/
│       ├── main.dart     بوابة الدخول حسب الدور
│       ├── api.dart      Api.base (http://IP:4000) — التاجر يغير IP هنا
│       ├── theme.dart    هوية «أفق» — الألوان الرسمية + A.glass الزجاجي
│       ├── models.dart   نماذج: Product, Store, LineItem…
│       ├── widgets.dart  مكونات مشتركة (كروت، شريط بحث…)
│       └── screens/      (13 شاشة تفصيلها تحت)
├── admin-dashboard/      لوحة آدمن ويب (HTML/JS نقي، RTL، هوية أفق)
├── infra/                قاعدة البيانات المحلية (PostgreSQL منفذ 5434)
└── landing/              صفحة هبوط (تعرض المشروع + زر تحميل APK من /download)
```

### الباك اند
- `express`, `pg` (مع `pool`), `socket.io` (التتبع اللحظي), `jsonwebtoken`, `bcryptjs`, `multer` (صور), `cors`, `dotenv`
- أوامر: `npm run dev` (--watch) · `db:init` · `db:migrate` · `db:seed`
- المصادقة: JWT — كل أدوات الـ routes تمر `auth()` ما عدا auth/public/health
- OTP: `DEV_OTP=true` في `.env` يطبع الرمز في رد الـ API بدل إرسال SMS

### قاعدة البيانات (36 جدول — أهمها)
- `users` (دور، حالة، نقاط), `governorates/d`istricts` , `addresses`
- `categories` + `category_attrs` (صفات منتجات لكل قسم), `stores` + `store_documents`
- `products` + `product_variants` + `offers` — التفصيل تحت
- `cart_items`, `coupons`, `orders` + `order_items` + `order_status_history`
- `delivery_trips` + `trip_orders`, `conversations` + `messages`, `reviews`, `product_questions`
- `wallets` + `wallet_transactions`, `point_transactions`, `spin_wins`, `cash_reports`, `refund_requests`
- `ad_packages` + `ad_requests`, `favorites`, `store_favorites`, `notifications`, `settings`, `audit_logs`, `delivery_track_log`

---

## ٣) التصميم 🎨 (هوية «أفق»)

### الألوان الرسمية (فئة `A` في `app/lib/theme.dart`)
| الثابت | القيمة | الاستخدام |
|---|---|---|
| `A.primary` | `#1D4ED8` | أزرق داكن — اللون الأساسي (هيدرات/أزرار) |
| `A.primaryDeep` | `#1E3A8A` | أزرق أعمق — تدرجات |
| `A.primaryLight` | `#38BDF8` | أزرق سماوي |
| `A.cyan` | `#06B6D4` | ثانوي فاتح |
| `A.accent` | `#F97316` | برتقالي شمسي — CTA/نقاط/محفظة |
| `A.accentDeep` | `#EA580C` | برتقالي غامق |
| `A.ink` | `#0A1120` | نصوص رئيسية |
| `A.bg` | `#FAF9F6` | خلفية دافئة فاتحة |
| `A.text` | `#101828` | نصوص |
| `A.muted` | `#475467` | نصوص ثانوية |
| `A.line` | `#E4E7EC` | حدود وخطوط |
| `A.success/warning/danger/info` | أخضر/كهرماني/أحمر/سماوي | حالات |
| `A.star` | `#F5A623` | تقييمات |

### اللغة والاتجاه
- **عربي بالكامل، RTL** (Directionality عربي)، خط عربي نظيف (افتراضي النظام)
- أزرار وأيقونات Material Rounded

### أسلوب «الزجاج» (Signature)
- `A.glass({radius, tint, soft, dark})` — **كروت زجاجية**: تدرج شفاف + حدود بيضاء شفافة (0.45) + ظل أزرق (`A.primary` شفافية 0.12) + زوايا مدورة
- تدرجات ثلاث: `A.gradNavy` (كحلي)، `A.gradSun` (برتقالي)، `A.gradSky` (سماوي)
- موتو: **«المال صلب، الجمال زجاج»** — الهوية ترف بزجاج شبه شفاف وتدرجات

### شاشات التطبيق (13)
1. `login_screen` — دخول/تسجيل بالأدوار
2. `shell` — سلة Navigator مشترك + `customer_shell` (زبون: رئيسي، متاجر، سلة، حساب — **طلباتي انتقل من التبويبات إلى: الهيدر الرئيسي + صفحة حسابي**)
3. `stores_screen` / `store_screen` — شبكة المتاجر + صفحة متجر (أقسام، راويت منتجات)
4. `category_products_screen` — قسم كامل بفلترة
5. `cart_screen` — سلة (كوبون، نقاط، تأكيد)
6. `orders_screen` — طلباتي بخط زمني
7. `delivery_shell` — مندوب: رحلات، تتبع خريطة، تسليم
8. `vendor_shell` — تاجر: منتجاتي، إضافة منتج بتركيبات، طلبات واردة

### الرئيسية «مثل شي إن» (بدون صفحات فرعية)
- اختيار فئة من الشريط **يحدّث الشبكة بنفس الصفحة** (يبقى البحث + الفئات + السلايدر ظاهرين) — الضغط على نفس الفئة يرجع للكل (لا زر «الكل»)
- البحث **حقيقي بنفس الصفحة** (TextField) — يشتغل مع الفئة والفلاتر معاً
- زر `tune` بأيقونة الفلتر فوق الشبكة: ترتيب + سعر + ألوان/مقاسات + عروض + شرائح نشطة بـ ✕
- متاجر: نفس هيدر الرئيسية (واسط · الكوت + إشعارات) + قسم **«الشريك»** أعلى القائمة = المتاجر المتابعة (للمسجل)
- اتصال المندوب: فقط **أثناء `delivering`** في صفحة الطلب (chat + tel) — حُذف زر الاتصال من صفحة المتجر
- حسابي: قوائمه الآن «طلباتي 📦 / نقاطي / عناويني» فقط (حُذفت: المتاجر المتابعة، مفضلتي، الإشعارات، السلة)
9. `admin_shell` — آدمن (اتفاق مع لوحة الويب)
10. `account_screen`, `notifications_screen`, `chat_screen`, `points_screen`, `map_screen`

### المحادثات — فقط زبون ↔ مندوب أثناء التوصيل
- **حُذفت نهائياً** محادثات الزبون/التاجر (روابط `customer.js`/`vendor.js` القديمة + تبويب «المحادثات» عند التاجر + زر «محادثة» في صفحة المتجر)
- `conversations` = `(user_id زبون، courier_id مندوب)` فريد — لا رابط بالمحل
- تُفتح من بطاقة المندوب في الطلب **بحالة `delivering` فقط** (زر 💬) أو من قائمة المندوب (أيقونة 💬 بالهيدر)
- أي رسالة/قراءة/قائمة تتحقق: **يوجد طلب `delivering` بين الطرفين** — بعد التسليم تُقفل وتختفي من الطرفين (403)
- `delivery_shell` يرسل بدور `courier`، والزبون بدور `customer`؛ إشعار صنفه `chat`

### كروت المنتج (`_ProdCard` في widgets)
- صورة + شارة قسم + قلّة `-x%`، سعر + سعر قديم مشطوب، **نقاط ألوان** (تمثّل تركيبات العرض مثل شي إن — ≤4 نقاط + `+n`)
- سلكة سريعة (+): منتج بمتغيرات → يفتح صفحة المنتج لاختيار التركيبة؛ بدون متغيرات → يضيف فوراً
- التجربة الرائعة: «شي إن» استيل — بطاقات مربعة، صور كبيرة، نقاط ألوان، سلة سريعة

---

## ٤) تركيبات المنتجات (آخر تطور — مهم جداً) 🧵

**المفهوم**: كل صف في `product_variants` = **تركيبة كاملة (لون + مقاس + كمية مستقلة)**.

| id | product_id | vgroup | color | name | stock |
|---|---|---|---|---|---|
| 1 | 5 | قياس | أحمر | 32 | 10 |
| 2 | 5 | قياس | أسود | 36 | 8 |
| 3 | 5 | قياس | (فارغ) | قياسي | 0 |

- **أحمر/32 موجود بـ 10 — أسود/32 غير موجود أصلاً** (ما يظهر للزبون)
- منتج بلا ألوان: `color=''` واسم مقاس واحد مثل `قياسي` أو `(فارغ)`
- `UNIQUE (product_id, vgroup, color, name)` — لا تكرار للتركيبة نفسها

### التدفق
- **تاجر** (`vendor_shell.dart` — صفحة إضافة/تعديل منتج): قسم «الألوان والمقاسات» = صفوف لكل تركيبة: حقل لون (سطر طويل) + مقاس (82px) + كمية (66px) + زر ✕، وزر **`+ أضف`** — يحفظ كقائمة `[{id?, color, name, stock}]` (PATCH يحذف غير الواردة)
- **زبون** (صفحة المنتج): يختار **اللون أولاً** → تظهر **مقاسات ذلك اللون فقط** بكمياتها؛ التركيبة الفارغة اللون تعرض المقاسات مباشرة؛ صف بـ stock=0 يظهر «نفد» وغير قابل للاختيار
- تُرسل السلة `{product_id, variant_id, variant_label}` — التسمية `color · name` (مثل «أحمر · 32`) — والطلب يخبر التركيبة

---

## ٥) دورة طلب كاملة 🛒

`سلة (POST /api/customer/cart)` → `تأكيد (POST /api/customer/orders)` → التاجر يقبل/يرفض → «تم تجهيزه» → مندوب يستلم → توصيل بتتبع لحظي (`socket.io`) → تسليم → محفظة التاجر تزداد → نقاط الزبون

---

## ٦) التشغيل ⚙️

```bash
# DB (مرة واحدة فقط)
cd infra && initdb -D pgdata -U postgres --auth=trust > /dev/null
pg_ctl -D pgdata -o "-p 5434 -k /tmp" -l pg.log start

# Backend
cd backend && npm install && npm run db:init && npm run db:seed && npm run dev
# → http://localhost:4000 (لوحة الآدمن على /admin)

# App
cd app && flutter pub get && flutter build apk --debug
# في app/lib/api.dart: غيّر Api.base لـ IP جهازك (http://IP:4000)
adb install -r build/app/outputs/flutter-apk/app-debug.apk
```

### حسابات
| الدور | الهاتف | الدخول |
|---|---|---|
| آدمن | `07900000000` | `admin123` |
| زبون / تاجر / مندوب | `07731234567` / `07701112233` / `07705556677` | OTP (يظهر في رد الـ API لـ DEV_OTP) |
| حسابات اختبار كلمة سر | تاجر `000000000100` / زبون `000000000200` | `123456` |

---

## ٧) كيف تستخدمني (Claude AI) 👇

أي سؤال عن المشروع أنا جاهز له، أمثلة:
- «كيف أعمل الإجراء X؟» / «لماذا يحدث خطأ Y؟» / «طور لي كذا والكود»
- «صمم لي شاشة جديدة بهوية أفق الزجاجية»
- «أضف خاصية كذا عبر الدور كذا — أين أعدّل؟»

الرد على أي استفسار بأكو نظرة كاملة على: الفكرة، 36 جدول، الروترز، شاشات Flutter الـ 13، والهوية «أفق» الزجاجية.