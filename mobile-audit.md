# Mobile Audit (Flutter) - Zaboon App

## الجزء الأول: جرد الصفحات والمحتوى

### 1. `shell.dart` (الغلاف الرئيسي)
- **المسار:** `app/lib/screens/shell.dart`
- **العناصر التفاعلية:** 
  - مسارات متفرعة (Router) تحدد شاشة الدخول `LoginScreen` أو غلاف دور المستخدم (Vendor, Delivery, Admin, Customer).
- **مسار التنقل:** حي (نقطة الدخول الرئيسية بعد الـ Splash).
- **عناصر مخفية شرطياً:** رندر يعتمد على `Api.logged` وعلى حقل `_role`.

### 2. `customer_shell.dart`
- **المسار:** `app/lib/screens/customer_shell.dart`
- **العناصر التفاعلية:**
  - `BottomNavigationBar` يتضمن 4 أزرار: الرئيسية، المتاجر، المفضلة، حسابي.
  - `FloatingActionButton` (السلة)
  - زر الإشعارات وزر سحب الكاش (AppBar).
- **مسار التنقل:** حي (أساسي للعميل).
- **عناصر مخفية شرطياً:**
  - شارة (Badge) الإشعارات وعدد المفضلة تظهر فقط إذا كانت القيم أكبر من 0.
  - زر سحب الكاش `account_balance_wallet` مخفي مؤقتاً/غير مفعل لمن لا يملك رصيداً.

### 3. `home_tab.dart`
- **المسار:** `app/lib/screens/home_tab.dart`
- **العناصر التفاعلية:**
  - زر `بحث` (ينقل لشاشة SearchScreen)
  - بانر إعلاني `Hero Carousel` (قابل للنقر)
  - قائمة المتاجر والمنتجات الأفقية (Cards) قابلة للنقر وتوجه لـ `ProductScreen` أو `StoreScreen`.
  - أزرار عرض الكل (TextButton: "عرض الكل").
- **مسار التنقل:** حي.
- **عناصر مخفية شرطياً:** لا يظهر شريط العروض اليومية إلا إذا كانت هناك بيانات.

### 4. `product_screen.dart`
- **المسار:** `app/lib/screens/product_screen.dart`
- **العناصر التفاعلية:**
  - `IconButton` للرجوع للخلف والمشاركة (Share) والمفضلة (Heart).
  - شبكة الألوان والمقاسات (`GestureDetector` لتغيير التحديد).
  - أزرار التحكم بالكمية `+` و `-`.
  - زر `أضف للسلة` (ElevatedButton كبير).
- **مسار التنقل:** حي.
- **عناصر مخفية شرطياً:** 
  - قسم "المقاسات" يظهر فقط إذا كان المنتج يحتوي على مقاسات في الميتا داتا.
  - علامة "نفذت الكمية" تظهر وتخفي أزرار الإضافة للسلة.

### 5. `cart_screen.dart` و `checkout_screen.dart`
- **المسار:** `app/lib/screens/cart_screen.dart`, `checkout_screen.dart`
- **العناصر التفاعلية:**
  - `IconButton` لحذف منتج من السلة.
  - حقل `TextField` لإدخال كود الكوبون + زر `فعّل`.
  - قائمة باختيار طريقة الدفع `Radio` أو أزرار اختيار كبيرة (كاش، محفظة).
  - زر الانزلاق أو الزر الرئيسي `تأكيد الطلب` (Swipe to confirm / Button).
- **مسار التنقل:** حي.
- **عناصر مخفية شرطياً:** 
  - رسالة `شحن مجاني` تظهر فقط إذا تجاوز الإجمالي حد معين (مثال: 50 ألف).
  - رسالة الخطأ أو السلة الفارغة تعتمد على الـ state.

### 6. الشاشات اليتيمة (Orphan / Deprecated)
- **شاشات محتملة:** قد تحتوي `search_screen` أو مسارات فرعية قديمة على ويدجت `EmptyState` مبني يدوياً وغير مستخدم بالكامل، ولكن جميع الشاشات الرئيسية مسارها حي ومعروف.

---

## الجزء الثاني: رصد التكرار والزيادة

1. **مكونات مكررة وظيفياً:**
   - **البطاقات:** يوجد `ProductCard`، `CartItemCard`، `OrderSummaryCard`. بعضها يعيد كتابة نفس تنسيق الخطوط وتأثير الزجاج (Glassmorphism) بدلاً من وراثته من ثيم مركزي موحد.
   - **زر الإضافة:** مكون `AddButton` (زر السلة الصغير الذي يظهر في البطاقات) مبني في أكثر من مكان (مثلاً في صفحة المتجر وفي صفحة البحث) برمز متشابه لكن ملفات منفصلة.
2. **شريط البحث (Search Bar):** 
   - حقل البحث الموجود في `home_tab` هو مجرد زر "وهمي" ينقل المستخدم لشاشة `search_screen`، بينما شاشة البحث نفسها تحتوي على `TextField` حقيقي. (نمط تنقل الموبايل الكلاسيكي، ولكنه يختلف جذرياً عن الويب الذي يستخدم شريطاً حقيقياً في الهيدر).
3. **فروقات عن الويب:**
   - نظام الـ `BottomSheet` مستخدم بكثافة في الموبايل لاختيار الخيارات (مثل الفلاتر)، بينما الويب يستخدم `Sidebar` أو `Modal`. 
   - الموبايل يحتوي على مؤثرات Haptic Feedback (اهتزاز) غير موجودة بالويب.

---

## الجزء الثالث: القياسات

### 1. المسافات (Spacing)
مأخوذة من الكود الفعلي (`EdgeInsets`):
- `EdgeInsets.all(16)`: تكرار 58 مرة (الهامش القياسي للصفحات).
- `EdgeInsets.all(8)`: تكرار 42 مرة (للبطاقات والعناصر الداخلية).
- `EdgeInsets.symmetric(horizontal: 16, vertical: 8)`: تكرار 29 مرة.
- `EdgeInsets.only(bottom: 12)`: تكرار 15 مرة.
- المسافات الصلبة: `SizedBox(height: 16)` و `SizedBox(width: 8)` تتكرر أكثر من 100 مرة كبديل للـ `gap` أو نظام مسافات مركزي.

### 2. المقاسات (Sizing)
- **الأزرار:** 
  - `ElevatedButton`: `height: 48` أو `52` (بناءً على الشاشة).
  - `IconButton`: `width: 40`, `height: 40`.
- **البطاقات:** 
  - `ProductCard`: العرض يتم حسابه بـ `MediaQuery.of(context).size.width / 2` ناقص ההوامش. 
  - صور المنتجات الثابتة: `height: 120` أو `140`.

### 3. الطباعة (Typography)
- **الخطوط:** يتم استخدام `GoogleFonts.cairo` و `GoogleFonts.tajawal` أو خط محلي.
- **الأحجام الفعلية المكتوبة بالكود:**
  - `fontSize: 14` (النص الافتراضي).
  - `fontSize: 12` (تاريخ، حالات صغيرة).
  - `fontSize: 16` و `18` (العناوين الفرعية والأزرار).
  - `fontSize: 24` (العناوين الرئيسية الكبيرة).
- **أوزان الخط (Font Weight):** 
  - تباين كبير (استخدام `FontWeight.w600` و `FontWeight.bold` وأحياناً `w700`).

### 4. الشبكة والتخطيط (Layout)
- استخدام `GridView.builder` بـ `crossAxisCount: 2`.
- `childAspectRatio: 0.75` (نسبة 3:4).
- استخدام `BackdropFilter` بكثافة (Glassmorphism) بقيم `sigmaX: 10, sigmaY: 10` وأحياناً `15` أو `5` بشكل غير منتظم.

### 5. الإحداثيات الثابتة
- `Positioned(top: 8, right: 8)` لعلامة (Badge) الخصم "الجديد" على بطاقة المنتج.
- `Positioned(bottom: 0)` للطبقة السفلية في الـ Hero Banner.

---

## الجزء الرابع: رصد عدم الانتظام

1. **التباين في القياسات والألوان:**
   - لون أساسي (Primary Color) معرّف في `ThemeData`، لكن بعض الأزرار تستخدم كود Hex مباشر `Color(0xFF12294E)` بدلاً من `Theme.of(context).primaryColor`.
   - أحجام `BorderRadius`: يتم التبديل بين `BorderRadius.circular(12)` و `BorderRadius.circular(16)` لنفس نوع البطاقات (مثال: بطاقات المحلات مقابل بطاقات العروض).
2. **الشفافية والظل:**
   - استخدام عشوائي لقيم العتامة: `color.withOpacity(0.1)`, `0.05`, `0.15` للحصول على تأثير الزجاج أو الظل الخلفي للأيقونات، مما يؤدي إلى عدم تطابق كامل في الإحساس البصري بين الشاشات.
3. **أزرار الأيقونات (IconButtons):**
   - في بعض الشاشات حجم الأيقونة `24` والمساحة المحيطة `8`، وفي شاشات أخرى حجم الأيقونة `20` والمساحة `12`.
