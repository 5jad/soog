# Web Audit (React/Vite) - Zaboon Store

## الجزء الأول: جرد الصفحات والمحتوى

### 1. `Home.jsx`
- **المسار:** `store-app/src/pages/Home.jsx`
- **العناصر التفاعلية:**
  - زر `تسوّق العروض الآن` (CTA, primary color, يوجّه لصفحة العروض)
  - زر `عرض الكل` / `عرض أقل` (Toggle state, inline style)
  - أزرار شرائح الفئات (`chipg`, تفعيل الفلتر)
  - أزرار `مسح الكل` (Clear filters, text button, احمر/disc)
  - حقول الإدخال: حقلي السعر `min` و `max` (نوع number)
- **مسار التنقل:** حي (الصفحة الرئيسية `/`)
- **عناصر مخفية شرطياً:** 
  - `Hero` (Promo) يختفي إذا كان المستخدم يفعل الفلترة `gridMode`.
  - شريط الثقة `trust-row` يختفي عند البحث.
  - `Sheet` الفلترة يظهر فقط عند النقر على الفلتر (`fltOpen`).

### 2. `CartPage.jsx`
- **المسار:** `store-app/src/pages/CartPage.jsx`
- **العناصر التفاعلية:**
  - زر `+` و `-` لتعديل الكمية (أيقونات msm)
  - زر `x` لحذف المنتج (أيقونة msm، لون muted)
  - حقل إدخال `كود كوبون هذا المحل`
  - زر `فعّل` (Button outline صغير)
  - زر `إتمام الطلب` (Button large, primary)
- **مسار التنقل:** حي (`/cart`)
- **عناصر مخفية شرطياً:**
  - إذا لم يكن مسجلاً يعرض `Empty` مع زر `تسجيل الدخول`.
  - إذا السلة فارغة يعرض `Empty` مع زر `ابدأ التسوق`.

### 3. `Checkout.jsx`
- **المسار:** `store-app/src/pages/Checkout.jsx`
- **العناصر التفاعلية:**
  - أزرار اختيار وسيلة الدفع (كاش / زين كاش / نقاط)
  - أزرار اختيار نوع التوصيل (عادي / سريع)
  - حقل إدخال `ملاحظات الطلب` (Textarea)
  - زر `تأكيد الطلب` (Button large, primary)
- **مسار التنقل:** حي (`/checkout`)
- **عناصر مخفية شرطياً:**
  - قسم اختيار العناوين يظهر فقط إذا كان لدى المستخدم عناوين محفوظة.
  - شاشة النجاح (Step 3) تظهر فقط بعد نجاح الـ POST.

### 4. `ProductPage.jsx`
- **المسار:** `store-app/src/pages/ProductPage.jsx`
- **العناصر التفاعلية:**
  - أزرار الألوان والمقاسات (`vc`, `vs`)
  - زر `أضف للسلة` (Button primary)
- **مسار التنقل:** حي (`/product/:id`)
- **عناصر مخفية شرطياً:** يعرض `Loader` أثناء جلب البيانات.

### 5. `VendorDashboard.jsx` / `DeliveryDashboard.jsx`
- **المسار:** `store-app/src/pages/VendorDashboard.jsx` و `DeliveryDashboard.jsx`
- **العناصر التفاعلية:**
  - تبويبات التنقل العلوية (الطلبات، المنتجات، الإعدادات).
  - أزرار `قبول`، `رفض`، `تحديث الحالة`.
- **مسار التنقل:** حي (مخصص لأدوار معينة)
- **عناصر مخفية شرطياً:** تعرض `Empty` "هذه المنطقة مخصصة للتجار/المندوبين فقط" إذا كان الدور غير مطابق.

### 6. `ComingSoon.jsx`
- **المسار:** `store-app/src/pages/ComingSoon.jsx`
- **العناصر التفاعلية:**
  - زر `العودة للمتجر`
- **مسار التنقل:** صفحة "يتيمة" جزئياً، تُستخدم كعنصر نائب (Placeholder) للروابط غير المكتملة.

---

## الجزء الثاني: رصد التكرار والزيادة

1. **مكونات مكررة وظيفياً:**
   - يوجد `Empty` في `ui.jsx`، بينما توجد حالات فارغة مبنية يدوياً بأسلوب مختلف (Inline HTML) داخل بعض الشاشات مثل `Home` و `CartPage` قبل توحيدها.
   - نافذة تسجيل الدخول `LoginModal` مكررة وظيفياً كمنطق مصغر داخل صفحات تتطلب الدخول (`Orders`, `Favorites`).
2. **عناصر غير مستخدمة (Dead Code):**
   - كلاسات CSS مثل `.deal.hid` و `.pc-low0` موجودة في `styles.css` لكن لم تُرصد في أي JSX.
3. **التكرار في نفس الصفحة:**
   - زر "عربة التسوق" متاح في الـ Header وفي الـ BottomNav (في وضع الشاشات الصغيرة)، مما يؤدي إلى تكرار الحدث `onClick={() => nav('/cart')}` مرتين في نفس الهيكل.
4. **فروقات بين الويب والموبايل:**
   - **الويب:** يحتوي على Footer كامل وروابط "سياسة الاسترجاع".
   - **الموبايل:** يعتمد كلياً على الـ Bottom Nav، و Drawer الجانبي، بينما الويب يستخدم Header ثابت. زر التحديث "Swipe to Refresh" موجود بالموبايل ومفقود في الويب.

---

## الجزء الثالث: القياسات

### 1. المسافات (Spacing / Margins / Paddings)
استناداً إلى `styles.css` (Design Tokens):
- `--sp-1: 4px` (يستخدم 12 مرة للمسافات الدقيقة بين الأيقونة والنص)
- `--sp-2: 8px` (يستخدم 45+ مرة في الـ gaps وهوامش البطاقات)
- `--sp-3: 12px` و `16px` (تضارب في `styles.css` بين تعريف المتغيرات والاستخدام الفعلي Inline).
- `--sp-4: 16px` و `24px`.
- مسافات مضمنة (Inline): `style={{ marginTop: 14 }}` متكررة 8 مرات في الصفحات.

### 2. المقاسات (Sizing)
- **الأزرار:** 
  - `btn-sm`: 40px (min-height)
  - `btn`: 48px
  - `btn-lg`: 52px (تكرار 14 مرة)
- **البطاقات:** 
  - `ProductCard` صورة المنتج مبنية على `aspect-ratio: 3 / 4`.
  - `StoreCard` ارتفاع ثابت `132px`.
- **الأيقونات:** `17px`, `19px`, `22px`, `27px` (تباين عشوائي).

### 3. الطباعة (Typography)
- **الخطوط الأساسية:** `IBM Plex Sans Arabic`, `Cairo`, `El Messiri`.
- **الأحجام:**
  - `11px`, `12px` (للنصوص الثانوية / الشارات)
  - `14px` (`--t-ui`) (الأساسي)
  - `17px` (`--t-hd`)
  - `24px` (`--t-h1`)
  - التكرار: أحجام الخطوط مضبوطة كمتغيرات، لكن هناك تجاوزات (Overrides) بقيم صلبة (مثل `font-size: 72px` للرموز التعبيرية القديمة).

### 4. الشبكة والتخطيط (Grid/Layout)
- الحاوية الأساسية `max-width: 620px` و `880px` في الويب (مثل `Checkout` و `CartPage`) مما يجعلها ضيقة على شاشات سطح المكتب وتترك مساحات بيضاء واسعة.
- التخطيط الشبكي: `grid-template-columns: repeat(auto-fill, minmax(165px, 1fr))` للبطاقات (يتمدد على الشاشات الكبيرة لكن يظل بحجم بطاقات موبايل).

### 5. الإحداثيات الثابتة (Fixed Coordinates)
- `BottomNav`: `bottom: 0`, `position: fixed`.
- `FAB` (العربة الطائرة): `bottom: 92px`, `right: 16px`.
- `Toast`: `bottom: 92px`, `transform: translate(-50%, 14px)`.

---

## الجزء الرابع: رصد عدم الانتظام

1. **قيم متقاربة غير مبررة:**
   - استخدام `gap: 6px` و `gap: 7px` و `gap: 8px` بشكل عشوائي بين الأيقونات والنصوص في `Layout.jsx` و `ProductPage.jsx`.
   - أحجام خطوط `10.5px` و `9.5px` مستخدمة في فقاعات الدردشة (`chat-msgs`).
2. **اختلاف مقاسات العنصر الواحد:**
   - بطاقة "المتجر" تظهر بـ `height: 128px` في شريط الـ Home، و `height: 132px` في صفحة المتاجر.
3. **زخرفة عشوائية:**
   - ظلال `box-shadow` تستخدم rgba بقيم `0.07`, `0.09`, `0.22`, `0.28`, `0.3` بدون توحيد لمتغير ظل واحد (Opacity عشوائية).
   - حواف `border-radius`: `8px`, `12px`, `16px`, `20px`، وتجاوزات inline `borderRadius: '16px 16px 0 0'`.
